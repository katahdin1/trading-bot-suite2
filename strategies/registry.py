from strategies.spy_rsi import spy_rsi_strategy
from strategies.spy_momentum import spy_momentum_strategy

strategy_lookup = {
    "spy_rsi": spy_rsi_strategy,
    "spy_momentum": spy_momentum_strategy
}
